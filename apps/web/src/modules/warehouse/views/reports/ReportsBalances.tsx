import { PlaceholderView } from '../PlaceholderView';

export const ReportsBalances = () => (
  <PlaceholderView
    title='Отчёт: Остатки и движения'
    path='/warehouse/reports/kpis'
    description='Отчёт по остаткам и движениям будет перенесён в одном из следующих релизов.'
  />
);

export default ReportsBalances;
